import React from 'react'
import styled from 'styled-components'

const StyledBrand = styled.span`
    color: #0099ff;
    font-weight: bold;
    font-size: 1.2rem;
    letter-spacing: 1px;
`

const Brand = () => (
    <StyledBrand>minitter</StyledBrand>
)

export default Brand
